<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>05_array_index</title>
    <script>
      let array = [10, 20, 30, 40, 50];
      console.log(array); // 콘솔창에 배열 요소 출력력

      alert(array.length); // 5 출력

      for (let i = 0; i < array.length; i++) {
        alert(i + 1 + "번째 출력" + array[i]);
      }
    </script>
  </head>
  <body>
    <h1>05_array_index</h1>
    <p>● 데이터 10, 20, 30, 40, 50을 배열로 정의</p>
    <p>● 배열의 길이를 콘솔로 출력</p>
    <p>● for 반복문을 이용하여 알림창으로 각 요소를를 출력</p>
  </body>
</html>
